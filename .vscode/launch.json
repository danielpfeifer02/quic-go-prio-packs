{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package in Namespace",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${fileDirname}",
            "args": ["*.go", "relay"],
            "preLaunchTask": "Run in Namespace"
        }
    ]
}
